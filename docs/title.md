# Title

Notebooks can have a friendly title by using the `title` field in the notebook's metadata. For example:

```json
"title": "Lesson 4: Variables and Data Flow"
```

If a title field is not present, the notebook's filename will be displayed instead.